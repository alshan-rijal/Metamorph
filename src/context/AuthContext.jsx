/* eslint-disable react-refresh/only-export-components */
import { createContext, useContext, useEffect, useState } from 'react'
import { supabase } from '../lib/supabaseClient'

const AuthContext = createContext({ session: null, user: null })
export function AuthProvider({ children }) { const [session, setSession] = useState(null); useEffect(() => { if (!supabase) return undefined; supabase.auth.getSession().then(({ data }) => setSession(data.session)); const { data: listener } = supabase.auth.onAuthStateChange((_event, next) => setSession(next)); return () => listener.subscription.unsubscribe() }, []); const unavailable = () => Promise.resolve({ error: { message: 'Supabase is not configured. Add VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY to .env.local.' } }); const signIn = (email, password) => supabase ? supabase.auth.signInWithPassword({ email, password }) : unavailable(); const signUp = (email, password, fullName) => supabase ? supabase.auth.signUp({ email, password, options: { data: { full_name: fullName } } }) : unavailable(); const signInWithGoogle = () => supabase ? supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: window.location.origin } }) : unavailable(); const updateProfile = (fullName) => supabase ? supabase.auth.updateUser({ data: { full_name: fullName } }) : unavailable(); const updatePassword = (password) => supabase ? supabase.auth.updateUser({ password }) : unavailable(); const resetPassword = (email) => supabase ? supabase.auth.resetPasswordForEmail(email, { redirectTo: `${window.location.origin}/profile` }) : unavailable(); const signOut = () => supabase?.auth.signOut(); return <AuthContext.Provider value={{ session, user: session?.user || null, signIn, signUp, signInWithGoogle, updateProfile, updatePassword, resetPassword, signOut }}>{children}</AuthContext.Provider> }
export const useAuth = () => useContext(AuthContext)
